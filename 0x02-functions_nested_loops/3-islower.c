#include "main.h"
/**
 * _islower - checks for lowercase letters
 * Description: checks for lower case letter
 * @c: character used to check for the letter's case
 * Return: 0 or 1
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
