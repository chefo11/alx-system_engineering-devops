#include "main.h"
/**
 * print_alphabet_x10(void) - prints abs x10
 * Return: always 0
 */
void print_alphabet_x10(void)
{
	char j, i;

	for (i = 0; i <= 9; i++)
	{
		for (j = 'a'; j <= 'z'; j++)
		{
			_putchar(x);
		}
		_putchar('\n');
	}
}
